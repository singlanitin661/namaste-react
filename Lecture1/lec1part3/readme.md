The costliest thing in dom is dom manupulation
<br/>
```{}``` : The empty {} this was used to give attributes. 
Currently we have created a heading with no attribute , If we want to give
an id to this heading 
```{ id : "heading"}```